services:
  web:
    image: ghcr.io/<YOUR_GH_USER>/albear-t:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./app:/srv:ro          
      - ./docker/Caddyfile:/etc/caddy/Caddyfile:ro
    restart: unless-stopped